#include "libft.h"

char		*strnstr(const char *haystack, const char *needle, size_t len)
{
	haystack = haystack;
	needle = needle;
	len = len;
/*	int					*i;
	int					j;

	if(*_needle == '\0')
		return((char*)_haystack);
	while(*_haystack && len--)
	{
		j = 0;
		while()
		{
			
		}
	}
*/	return (0);
}
